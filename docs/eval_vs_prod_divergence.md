# 评测 / 生产 差异清单

**底线：最终验收时，产出那个数的配置必须就是上线的配置。这张清单最后要清零。**

只要这张表非空，对外报的 DA / TCR 就**不是生产当前行为的数**。
每加一项记一行；每消一项在"清零记录"里留痕，不要直接删行。

最后更新：2026-07-29

**当前对外主数**（Y3 候选重排序，≤199 字符档）：
`DA 0.9157（句级）/ 0.9266（段级）/ TCR 术语级 97.56%（全集 689 上重算）/ 样本级 95.05%`
基线并列（臂 A / 单配置 689 全量重译，非拼接）：`DA v2 0.8564 / v1 0.8440 / TCR 术语级 96.51%`
—— 均出自评测配置，与生产存在下列 **4 项**差异。

## 当前未清零项（4）

| # | 项 | 生产 | 评测 | 影响 | 如何清零 |
|---|---|---|---|---|---|
| 1 | **术语库版本** | `f00ba0de15476b22fbe8b73ad88fe64d`（T4 之前那一版） | `3b8221df26c70e1012cb367a6814f7fe`（T4 版，含 7 条改动） | T4 的 7 条改动（cell / high voltage / approval authority / compliance / manikin / shall / shall not）**从未上过生产**。对外的 **TCR 96.43% 是评测术语库的数**；同一批译文在生产术语库下重算为 **93.81%** | 术语库改动作为**独立部署批次**上线，同步更新 `selfcheck.py` 的 `EXPECTED_TERMBASE_MD5` 与 `README_DEPLOY §五` |
| 2 | **`match_terms` span 抑制修复** | 未同步（服务器副本按部署冻结） | 已含 S3a / S3a-2 的嵌套/别名 span 抑制（`abb91f1`） | 未修复版本**膨胀 TCR 分母**（嵌套与重复术语被重复要求）。历史上据此发布过 88.79%，已作废 | 随下一次 `scripts/` 部署同步；部署后用同一批译文重算 TCR 对齐两侧 |
| 3 | **`target_alias` 目标端别名支持** | 代码未部署、CSV 无该列 | `termbase.py` 支持 + CSV 有 `target_alias` 列（`b3228cf` / `804106d`） | 生产不认参考语料里与 target 并存的合法译法（`shall`→「需」、`shall not`→「不应」），**同样译文在生产口径下 TCR 偏低** | 与第 1 项同批上线（同一个 CSV + 同一份 `termbase.py`） |
| 4 | **Y3 候选重排序跑不了生产** | 单次 temp=0 贪心生成，一条一次调用 | 每条生成 4 候选（1 贪心 + 3 采样）→ TCR 硬检查 → QE 取最高 | **性质与前三项不同：前三项是"该上而未上"，这项是"上不去"。** 对外主数全部出自这个配置；6000 字符 × 4 候选会击穿 **120s 合同**。**退回臂A（单次生成）的真实代价见下表** | 三条候选路线，**均未验证**：① vLLM 单请求 `n=4`（共享 prefill、批量解码，比发 4 次便宜得多，最可能塞进预算）；② 按长度分档——短文本走重排序、长文本走单次，需重报分档数；③ 蒸馏重排序结果回单次生成。**验收前必须选定一条并实测 6000 字符 P99 延迟** |

前三项**同源**：都是"评测侧改了、生产侧按部署冻结没跟"。第 1、3 项可以一批上线；
第 2 项是 `scripts/` 侧代码，随任意一次部署带上即可。
**第 4 项不同源，也不能靠部署解决**——它是产出主数的那个配置本身超预算，
按上面的底线（"产出那个数的配置必须就是上线的配置"），这项不清零则 0.9157 不能作为验收数。

### 第 4 项清不掉的代价（退回臂 A 单次生成）

**句级全量是头条口径，先看它。**

| 口径 | Y3 重排序（现主数） | 臂 A（退回后） | 代价 |
|---|---:|---:|---:|
| **句级 全量** | **0.9009**（n=911） | **0.8864**（n=913） | **−0.0145** |
| 句级 ≤199 档 | 0.9157（n=665） | 0.9047（n=666） | −0.0110 |
| 句级 100–199 档 | 0.9106 | **0.8996** | −0.0110，**跌破 0.90** |
| 段级 全量 | 0.8782 | 0.8564 | −0.0218 |
| 段级 ≤199 档 | 0.9266 | 0.9114 | −0.0152 |
| TCR 术语级 | 97.56%（1119/1147） | 96.51%（1107/1147） | −1.05pp（仍远超 95% 门槛） |

**要害在 100–199 档**：退回臂 A 后该档 0.8996 跌破 0.90，而它是 ≤199 主口径里
占比最大的一档（句级 48.4%）。≤199 合计 0.9047 仍 ≥ 0.90，靠的是 ≤99 档 0.9146 拉平。
**即"达标"在退回后只剩 0.0047 的余量，且内部有一档是负的。**

三条候选路线的验收判据统一为：**6000 字符 P99 延迟 < 120s，且句级全量 DA ≥ 0.90**。

### 上线决定（2026-07-28）

- **第 1 项按 T4 版（`3b8221df`）上线**，不跳到 V2 版。
- **V2 的 13 条不上线**：净收益 +0.0015 < +0.005 判据，且 `approval mark` 明确改错。
- **`enclosure` 也不单独上线**：单条效应虽最明确（+0.0334），但在 34% 的实例上
  参考写的仍是旧译法「外壳」，改了会倒扣；且现在单挑它出来就是**按分数挑术语**。
  T5 的合并计数盲区尚未逐条排查完，它有没有同类问题未知。


## 为什么会积起来

生产与评测**共用同一个术语库文件**（`.env` 的 `TERMBASE_PATH=termbase/auto_regulation_terms_v1.csv`），
但服务器那份按部署冻结、本地那份随评测演进，两边就此分叉。

- 2026-07-28 起 `api/deploy/selfcheck.py` 每 10 分钟断言生产术语库 md5 不变
  （与 `reasoning_tokens == 0` 并列的外部漂移哨兵）。它**防的是"被无意改动"**，
  不解决"该上而未上"——那要靠这张清单。
- V2 的两臂都用 `/data/SERVICE_USER/translation-eval/tb/` 下的副本，
  `translate_local_npu.py --termbase` 指定，**不触碰生产那份**。

### 代码副本漂移（2026-07-29 查实，同型失败第四次）

术语库会分叉，**代码副本一样会**，而且没有 md5 哨兵盯着。服务器上有两份：

| 路径 | `scripts/common/termbase.py` md5 | 状态 |
|---|---|---|
| `PROJECT_ROOT`（下称 REPO） | `7ba3478a` | **冻结**，无 S3a span 抑制 |
| `/data/SERVICE_USER/translation-eval/localsrc` | `38cc5ef6` | 与本地当前版一致 |

两版 `match_terms` 在 689 条源文上的差：**34.8% 的源文匹配列表不同，硬术语数 1392 vs 1147（+21.4%）**。

- **生成侧**：`translate_local_npu.py`（臂A）、`y3_gen.py`、`c1_gen.py` **三者都解析到 REPO**
  （`translate_local_npu.py` 的候选路径里 `20260727/` 无 `scripts/`，直接回落到 REPO）。
  → 三臂注入的术语列表口径**一致**，臂A/Y3/C1 之间的对比干净；
  → 且该口径**恰好与生产一致**（生产也是未修复版，即第 2 项）。这是巧合，不是设计。
- **打分/TCR 侧**：已发布的 96.51% = **1107/1147**，用的是**修复版**、在**全集 689** 上算。
- **因此**：`/data/SERVICE_USER/translation-eval/c1_analyze.py` 引用 REPO 且在 627 上算 TCR，
  会得到 ≈79.5% 并把所有臂判为"否决"，**且不报错**。该脚本已作废，
  分析改为本地 `scripts/analysis/c1_analyze.py`，三条护栏（术语库 md5 / 硬术语数 1147 /
  臂A TCR 1107·1147 与 506·545）任一不符即抛错，不出数。

**教训**：凡是要出对外数字的脚本，不接受"能跑通"作为正确性证据，
必须先复现一个已知的已发布数才允许往下算。

## 验收前必做

1. 这张表清零；
2. 清零后**用上线配置重跑一次**评测，拿到的数才是可对外的数；
3. 报数时标注译文批次（`subset_report.py --batch-label` 已强制）。

**在清零之前，任何对外数字都必须附一句：该数出自评测配置，与生产当前配置存在 N 项差异。**

## 清零记录

（暂无）
